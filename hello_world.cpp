#include<iostream>//--> #include is a preprocessor directive and iostream is a 
                // header file use for input and output ?
using namespace std;
                //  |__> using namespace std means that you tell to the computer that 
                //  include every standard library components .

int main() //--> every program has a main() , because it's a kind of start of the program.
{
   cout<<"Hello, world ."; //--> here cout is the object of ostream(header file)."<<" is called
                           // insertio operator . cout stands for "character output" .it is used
                           //to convey your message to the console(screen).
}